import { useState } from 'react'
import { ArrowUpRight } from 'lucide-react'
import SocialLinks from '../components/SocialLinks'
export default function Contact() { const [form, setForm] = useState({ name: '', email: '', message: '' }); const update = (field) => (event) => setForm({ ...form, [field]: event.target.value }); const submit = (event) => { event.preventDefault(); const body = encodeURIComponent(`Name: ${form.name}\nEmail: ${form.email}\n\n${form.message}`); window.location.href = `mailto:ms2150125@gmail.com?subject=${encodeURIComponent(`Portfolio message from ${form.name}`)}&body=${body}` }; return <section className="section contact" id="contact"><div className="container contact-grid"><div><span className="section-index">09</span><div className="eyebrow">Get in touch</div><h2>Let's build something useful.</h2><p>I'm always interested in learning, building projects and connecting with people working in technology.</p><SocialLinks /></div><form className="contact-form" onSubmit={submit}><label>Name<input required value={form.name} onChange={update('name')} placeholder="Your name" /></label><label>Email<input required type="email" value={form.email} onChange={update('email')} placeholder="you@example.com" /></label><label>Message<textarea required rows="5" value={form.message} onChange={update('message')} placeholder="What are you working on?" /></label><button className="button" type="submit">Send Message <ArrowUpRight size={16} /></button><small>Opens your email client to send to ms2150125@gmail.com</small></form></div></section> }