import { ArrowUpRight, Code2, ExternalLink } from 'lucide-react'
import { motion } from 'framer-motion'
export default function ProjectCard({ project, index }) { return <motion.article className={`project-card project-card--${project.tone || 'blue'}`} whileHover={{ y: -7 }} transition={{ duration: .25 }}><div className="project-art"><Code2 size={28} /><span>0{index + 1}</span></div><div className="project-card__body"><div className="project-card__meta">{project.category}<span>{project.status}</span></div><h3>{project.title}</h3><p>{project.description}</p>{project.features && <ul>{project.features.map((feature) => <li key={feature}>{feature}</li>)}</ul>}</div><div className="project-card__footer"><div className="tags">{project.tags.map((tag) => <span className="tag" key={tag}>{tag}</span>)}</div><div className="project-links"><a href={project.github} aria-label={`${project.title} GitHub`}><Code2 size={16} /></a>{project.demo && <a href={project.demo} aria-label={`${project.title} live demo`}><ExternalLink size={16} /></a>}<ArrowUpRight size={17} className="project-arrow" /></div></div></motion.article> }