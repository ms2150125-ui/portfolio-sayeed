import { ArrowUpRight, Code2, ExternalLink } from 'lucide-react'
export default function ProjectCard({ project, index }) { return <article className="project-card" data-rgb-tooltip={`Project · ${project.title}`}><div className="project-card__body"><div className="project-card__meta"><span>{project.category}</span><span>{project.status}</span></div><h3>{project.title}</h3><p>{project.description}</p>{project.features && <ul>{project.features.map((feature) => <li key={feature}>{feature}</li>)}</ul>}</div><div className="project-card__footer"><div className="tags">{project.tags.map((tag) => <span className="tag" key={tag}>{tag}</span>)}</div><div className="project-links"><a href={project.github} aria-label={`${project.title} GitHub`}><Code2 size={16} /></a>{project.demo && <a href={project.demo} aria-label={`${project.title} live demo`}><ExternalLink size={16} /></a>}<span className="project-number">{String(index + 1).padStart(2, '0')}</span><ArrowUpRight size={17} className="project-arrow" aria-hidden="true" /></div></div></article> }